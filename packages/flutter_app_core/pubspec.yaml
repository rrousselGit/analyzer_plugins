name: flutter_app_core
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  my_analyzer_plugin:
    path: ../my_analyzer_plugin
  another_plugin:
    path: ../another_plugin
  flutter_test:
    sdk: flutter
