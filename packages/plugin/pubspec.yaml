name: plugin
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  plugin_config:
    path: ../plugin_config
  pubspec_parse: ^1.0.0
  cli_util: ^0.3.0

executables:
  plugin: main